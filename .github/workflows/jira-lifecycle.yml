name: 🏗️ Jira Lifecycle (Infra)

on:
  pull_request:
    types: [opened, closed]
    branches: [staging, main, master]
  push:
    branches: [staging, main, master]

jobs:
  update-jira:
    uses: T-ESP-ASSTAGJ/infra/.github/workflows/_reusable/jira-lifecycle.yml@main
    with:
      repository_name: "infra"
    secrets:
      JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}