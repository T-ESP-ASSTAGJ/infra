apiVersion: v1
kind: Service
metadata:
  name: openbsd-metrics
  namespace: openbsd
  labels:
    app: openbsd
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 9107
